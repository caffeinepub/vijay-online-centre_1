{
  "frontend/src/pages/AdminPage.tsx": "Full admin panel with login (vijay@123 / vijay@123456), three tabs (Active Requests with CONFIRM/REJECT buttons and document previews, Payments to Verify with DONE-only button, All Applications table), and inline document thumbnail/download rendering using ExternalBlob.getDirectURL().",
  "frontend/src/hooks/useQueries.ts": "Comprehensive hooks file combining all legacy localStorage-backed hooks (Service, Application, Document types; useServices, useApplications, useDocuments, useUploadDocument, useChatbot) with all backend-integrated hooks for the \"Upload First, Pay Later\" flow (useGetApplicationById, useGetApplicationsByStatus, useGetAllApplications, useAddApplication, useSetApplicationFee, useRejectApplication, useConfirmPayment, useMarkPaymentPendingVerification, useGetRejectionMessage, useAuthenticate, user profile hooks).",
  "frontend/src/pages/PaymentPage.tsx": "Multi-step customer payment flow — Step 1 form with document upload, Step 2 waiting screen polling for admin review, Step 3 shows UPI QR only after priceSet status, Step 4 verifying spinner, Step 5 success receipt, and a Rejected step showing the rejection message when the application is deleted by admin.",
  "frontend/src/App.tsx": "Root app component wrapping everything in QueryClientProvider; manages page/service state, wires Header/Footer/FloatingSupport/ChatbotWidget, and renders the correct page — now correctly passes `service={selectedService?.name ?? ''}` to PaymentPage to satisfy the required `service` prop."
}