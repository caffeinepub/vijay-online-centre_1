{
  "frontend/src/pages/AdminPage.tsx": "Admin dashboard with live stats (Total/Active/Completed/Paid), searchable/filterable customer table with inline edit controls, a new \"Mark Paid\" button per customer row that calls `useMarkPaymentSuccess`, payment status column showing ✅ Paid badge with receipt ID and timestamp or ⏳ Pending, and a \"Receipt\" link for paid customers.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all customer backend operations — adds `useMarkPaymentSuccess` mutation (calls `actor.markPaymentSuccess(BigInt(id))` and invalidates both customers query keys) and `useGetAllCustomers` public query hook; all existing hooks updated to also invalidate `customers-public` query key on success.",
  "frontend/src/pages/PaymentPage.tsx": "Three-step customer flow (form → UPI payment → success) with an added auto-redirect countdown to ReceiptPage after success, a \"View Receipt Now\" button, and `onNavigate('receipt', { customerId })` call to pass the customer ID to the receipt page.",
  "frontend/src/App.tsx": "Root app with QueryClientProvider, page-based routing (home/payment/admin/receipt), admin auth gate, and AdminLoginModal with the required `isOpen={showAdminLogin}` prop now correctly passed; Header/Footer shown on non-admin pages.",
  "frontend/src/hooks/useCustomerAuth.ts": "Customer auth hook with 30-day localStorage session; exports `generateRandomOTP()`, `getLatestAuth()`, and the `useCustomerAuth` hook which returns both `loginWithMobile`/`login` aliases, `logout`, `name`, `mobile`, `identifier`, `isAuthenticated`, and `auth` object — restoring all previously expected exports.",
  "frontend/src/components/CustomerAuthModal.tsx": "Full-screen login modal with Navy Blue header; generates a fresh random OTP on every \"Get OTP\" click, displays it prominently in the UI for demo purposes, and only enables the Verify button when the entered OTP exactly matches the generated one — all references to the static \"123456\" OTP removed.",
  "frontend/src/components/ProgressTracker.tsx": "Responsive 5-stage progress tracker component (Request Received → Documents Verified → Payment Confirmed → Work in Progress → Completed) with desktop horizontal bar and mobile vertical list views.",
  "frontend/src/components/FloatingSupport.tsx": "Floating support widget with chat panel using `msg.isUser` (not `msg.role`) for message bubble alignment and styling.",
  "frontend/src/pages/HomePage.tsx": "Home page with hero banner, search bar, and a responsive grid of all 32 services with icons and categories; clicking a service navigates to the payment form.",
  "frontend/src/pages/DashboardPage.tsx": "Application tracking dashboard that passes `currentStage` (not `stage`) to `ProgressTracker`, shows fee from `app.price`, and includes UPI payment submission with transaction ID input.",
  "frontend/src/components/Header.tsx": "Sticky navy header with logo, desktop/mobile navigation, Admin Login button (shows modal when not authenticated), Dashboard link and Logout button when authenticated.",
  "frontend/src/index.css": "OKLCH-based design system with deep navy background, saffron/orange primary, Inter + Poppins fonts, status color tokens, and fade-in animation.",
  "frontend/tailwind.config.js": "Tailwind config with full navy scale (50–950), saffron palette, status-active/status-completed tokens, Poppins display font, custom border-radius, and navy/saffron box-shadow utilities.",
  "frontend/src/components/ChatbotWidget.tsx": "Floating chatbot widget using `msg.isUser` (not `msg.role`) to determine message alignment and styling.",
  "frontend/src/hooks/useAdminAuth.ts": "Admin authentication hook that validates credentials client-side first (vijay@123/vijay@2026) then optionally calls backend, stores VIJAY_ADMIN_TOKEN and role=admin in localStorage with 24-hour expiry, and exposes login/logout/getToken/isAdminAuthenticated helpers — correct credentials always succeed.",
  "frontend/src/components/AdminLoginModal.tsx": "Admin login modal with username/password fields, show/hide password toggle, error display, loading state, and calls useAdminAuth.login() — redirects to dashboard on success with navy/saffron theme.",
  "frontend/src/components/Footer.tsx": "Navy footer with brand info, popular services list, contact details including UPI ID, copyright year, and caffeine.ai attribution link.",
  "frontend/src/pages/ReceiptPage.tsx": "New payment receipt page that displays a clean white printable receipt with Vijay Online Centre branding, customer/payment details, UPI ID, receipt number, and green SUCCESS badge; supports mobile number search, auto-polls every 5 seconds when payment is pending, and provides Print Receipt and Download Slip buttons with `@media print` CSS to hide non-receipt elements."
}