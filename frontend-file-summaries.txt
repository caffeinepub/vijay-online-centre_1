{
  "frontend/src/pages/AdminPage.tsx": "Admin dashboard with force-refresh logic using queryClient.invalidateQueries + removeQueries + refetch, live-computed stats from fetched application data (no hardcoded counts), Set Price handler with admin token validation and Unauthorized error display, and mobile-friendly card layout.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend operations plus the restored `useChatbot` hook with FAQ-based bot responses; all queries use `staleTime:0` and `gcTime:0` to prevent stale cache on mobile.",
  "frontend/src/pages/PaymentPage.tsx": "Multi-step application form (details → upload → submitting → submitted) with corrected submission logic using a single object payload; includes graceful blob-error fallback to no-docs submission, ICICI Bank QR, and UPI link `8173064549@okicici`.",
  "frontend/src/App.tsx": "Root app component with page routing and admin auth gate; uses `isAdminAuthenticated` as a boolean property (not a function call) to guard the admin page and show the login modal.",
  "frontend/src/hooks/useCustomerAuth.ts": "Customer auth hook with 30-day localStorage session; exports `generateRandomOTP()`, `getLatestAuth()`, and the `useCustomerAuth` hook which returns both `loginWithMobile`/`login` aliases, `logout`, `name`, `mobile`, `identifier`, `isAuthenticated`, and `auth` object — restoring all previously expected exports.",
  "frontend/src/components/CustomerAuthModal.tsx": "Full-screen login modal with Navy Blue header; generates a fresh random OTP on every \"Get OTP\" click, displays it prominently in the UI for demo purposes, and only enables the Verify button when the entered OTP exactly matches the generated one — all references to the static \"123456\" OTP removed.",
  "frontend/src/components/ProgressTracker.tsx": "Responsive 5-stage progress tracker component (Request Received → Documents Verified → Payment Confirmed → Work in Progress → Completed) with desktop horizontal bar and mobile vertical list views.",
  "frontend/src/components/FloatingSupport.tsx": "Floating support widget with chat panel using `msg.isUser` (not `msg.role`) for message bubble alignment and styling.",
  "frontend/src/pages/HomePage.tsx": "Home page with all 32 services in 7 categories, search/filter bar, and correct CustomerAuthModal usage with `isOpen`, `onClose`, `onAuthenticated` props; accepts `onNavigate` prop from App.tsx.",
  "frontend/src/pages/DashboardPage.tsx": "Application tracking dashboard that passes `currentStage` (not `stage`) to `ProgressTracker`, shows fee from `app.price`, and includes UPI payment submission with transaction ID input.",
  "frontend/src/components/Header.tsx": "Sticky Navy Blue header with logo, desktop nav, mobile hamburger menu (inline dropdown, no z-index overlap), customer login/logout, and Manager Panel link; mobile menu closes on any navigation action.",
  "frontend/src/index.css": "OKLCH-based design system updated with Navy Blue (#002147) as the primary color token for both light and dark modes, with semantic color variables and fade-in animation.",
  "frontend/tailwind.config.js": "Tailwind config with Navy Blue color scale (navy-50 through navy-900), Inter font family, navy box-shadow utilities, and tailwindcss-animate plugin.",
  "frontend/src/components/ChatbotWidget.tsx": "Floating chatbot widget using `msg.isUser` (not `msg.role`) to determine message alignment and styling.",
  "frontend/src/hooks/useAdminAuth.ts": "Admin authentication hook with hardcoded credential pairs (vijay/123 and vijay/2026), 24-hour localStorage session management, and helper functions for login, logout, token retrieval, and session validation.",
  "frontend/src/components/AdminLoginModal.tsx": "Admin login modal that awaits the async `login()` promise and destructures `result.success` to determine whether to call `onSuccess` or display the error message."
}